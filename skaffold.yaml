apiVersion: skaffold/v2alpha3
kind: Config
deploy:
  kubectl:
    manifests:
      - ./infra/k8s/*
sync:
  manual:
    - dest: .
      src: 'src/**/*.ts'
build:
  artifacts:
    - image: kashiish19/auth
      context: auth
      docker:
        dockerfile: Dockerfile
        network: host